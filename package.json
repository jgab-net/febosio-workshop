{
  "name": "workshop-a",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "lerna run build --no-private",
    "clean": "lerna clean",
    "test": "lerna run test --no-private",
    "publish": "lerna publish --no-private --loglevel verbose",
    "start:app-1": "pnpm run --filter=@febosio/app-1 dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lerna": "^8.0.0"
  }
}
